name: Generate-Trusted-Templates
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:  
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Azure Login
        uses: Azure/login@v1.1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Context Setup
        run: |
          cd ./src/template-spec-bicep
          chmod +x ./setup-localmachine.sh
          ./setup-localmachine.sh
      - name: Install Bicep CLI
        run: |
          cd ./src/template-spec-bicep/paas-components
          chmod +x ./install-bicep.sh
          ./install-bicep.sh
      - name: Compile Bicep Scripts
        run: |
          cd ./src/template-spec-bicep/paas-components
          chmod +x ./build-templates.sh
          ./build-templates.sh
